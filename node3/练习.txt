1.使用express的模板引擎技术（随便ejs或者是arttemplate）技术，渲染出一个背景色是蓝色的导航出来，导航里面的文字必须是来自下面这个对象
{
 navlist:['首页','汽车','婚庆','母婴','交友']
}

2.创建一个包涵路径/index,/list,/content对应响应内容的路由出来，并且把这个路由单独封装在一个模块router.js里面

3.利用自定义中间件的技术，在req对象上挂载一个user对象，该对象里面的属性是姓名和年龄,然后这个req.user对象需要在之后的中间件里面都可以访问的到